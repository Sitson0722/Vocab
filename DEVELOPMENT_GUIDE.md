# Vocab Android 应用开发指南

> 本文是交给 Codex 的产品与开发任务书。重点是实现目标、学习逻辑和验收结果；具体代码结构与库由实现者合理决定。

## 1. 项目目标

在仓库 <https://github.com/Sitson0722/Vocab> 中开发一款 Android 英语词汇学习应用。

它不是固定例句的单词卡，而是一套以间隔重复为调度基础、以短语和语境为学习单位、由 AI 持续生成变化材料的主动学习系统。系统必须分别判断用户是否“看得懂”和“说得出/写得出”，并针对薄弱维度安排后续练习。

用户不配置本地 Android 开发或构建环境。仓库必须能够仅依靠 GitHub Actions 完成检查、测试和 APK 构建，并能按版本标签生成可下载的发布包。

## 2. 技术方向

- 目标平台：Android。
- 推荐技术：Kotlin + Jetpack Compose，采用现代 Android 单应用架构。
- 数据优先保存在本机，使核心学习流程离线可用；需要 AI 的内容生成允许联网。
- AI 服务必须通过可替换的接口接入，不把供应商、模型或密钥写死在客户端代码中。
- 所有密钥通过安全配置或 GitHub Secrets 注入，不得提交到仓库。
- 项目应包含 Gradle Wrapper，使 GitHub Actions 能从干净环境直接构建。

## 3. 核心学习对象

学习对象不是只有一个字符串形式的单词，而是以下层级：

- 词条：词形、词元、语言、基础元数据。
- 词义：该词的一项明确含义。多义词的不同含义必须分开建模。
- 短语/搭配：该词义下常见且自然的搭配、固定表达或句法框架。
- 语境：体现特定词义和搭配的句子、短对话或微型情境。
- 学习材料：某次练习实际呈现的题目、提示、答案及解释。
- 掌握状态：用户对具体词义及其短语/语境的理解与产出能力。

同一单词的不同词义不能混成一个进度，也不能因表述不同被重复创建。系统应为每个词义保留稳定标识，并维护其已覆盖、待覆盖和已掌握的短语与语境范围。

## 4. 学习与调度逻辑

### 4.1 间隔重复

每个可学习单元都要有独立的复习状态，包括至少：到期时间、间隔、难度或稳定度、最近结果、连续成功/失败情况和复习历史。

调度器应遵循以下原则：

- 新内容与到期复习内容分开管理。
- 回答正确且稳定时逐步延长间隔；错误、犹豫或依赖提示时缩短间隔。
- 反复出错的内容应增加近期曝光，但避免短时间机械轰炸。
- 调度结果必须可解释、可持久化，并能在应用重启后保持一致。
- AI 只负责生成和分析内容，不得直接决定复习日期或任意修改掌握度；确定性的本地学习引擎拥有最终决定权。

### 4.2 二维掌握度

每个词义至少维护两个相互独立的维度：

1. 理解（comprehension）：看到或听到该表达时能否识别正确含义。
2. 产出（production）：给定含义或语境时能否主动说出、写出或选用正确表达。

不能用理解正确代替产出掌握。产出通常应有更严格的达标要求，且两类练习分别影响对应维度；必要时可有少量跨维度增益，但不能使未练习维度自动毕业。

一个词义只有在两个维度均达到阈值，并且在不同语境与不同时间间隔下表现稳定，才可视为掌握。总体词条掌握度由各词义覆盖情况汇总，不能因掌握最常见词义而忽略其他目标词义。

### 4.3 短语与语境学习

- 优先教授自然、常用、能区分词义的短语和搭配，而不是孤立词义对照。
- 每次题目应明确对应一个目标词义，但可通过真实语境考查。
- 学习顺序优先高频、实用词义与搭配，再逐步扩展长尾义项。
- 语境必须足以消除歧义，答案和解释应与目标词义一致。
- 用户应能查看当前词义、典型搭配、简洁释义和必要说明，但学习主流程保持轻量。

### 4.4 多义词覆盖与去重

系统必须同时解决“遗漏”和“重复”：

- 为每个词条维护规范化义项清单及覆盖状态。
- 新生成内容必须绑定已有义项；确需新增义项时先经过规范化与去重检查。
- 相同含义的细微改写不得被当作新义项反复教学。
- 不同含义不得因为共享词形而合并评分。
- AI 生成前向其提供已覆盖义项、短语和近期材料摘要，生成后再做语义重复、目标义项和质量校验。
- 用户可看到还有哪些目标义项未覆盖，并可纠正错误归类。

### 4.5 防止记住固定题目

系统考查的是词义迁移能力，不是对固定例句的记忆：

- 同一知识点重复出现时，改变人物、场景、句式、语气、时态和干扰项。
- 不连续重复完全相同的题面；近期材料需要去重。
- 关键搭配和语义约束保持稳定，表面表达持续变化。
- 掌握判定必须基于多个不同语境中的表现，而非一条例句连续答对。
- 保留生成材料指纹或相似度信息，用于避免近似改写式重复。

### 4.6 自适应复习

每次选题综合考虑：到期程度、词义重要性、两个掌握维度、历史错误类型、提示依赖、反应时间、近期重复度和内容覆盖缺口。

系统应优先安排：

- 已到期且即将遗忘的内容；
- 理解强但产出弱，或产出偶然正确但理解不稳的内容；
- 容易与其他词义、近义词或搭配混淆的内容；
- 尚未在足够多样语境中验证的内容；
- 多义词中尚未覆盖或长期薄弱的目标义项。

难度变化应平滑。一次错误不能抹掉全部长期进度，一次猜对也不能造成大幅晋级。

## 5. 交互模式

MVP 至少包含以下模式，并允许混合成每日学习队列：

- 理解选择：给出带充分语境的表达，选择或判断正确含义。
- 语境辨义：同一词的多个义项中，根据句子或短对话识别当前义项。
- 填空产出：给出含义和语境，主动输入目标词或短语。
- 提示产出：允许逐级提示；使用提示后仍可完成，但评分低于无提示完成。
- 搭配选择/补全：判断自然搭配或完成短语框架。
- 自评模式：适用于无法可靠自动判定的开放式口头或自由产出，展示参考答案后由用户评价“不会/困难/正确/轻松”。

系统应记录正确性、用时、提示次数和错误类型。输入判定允许合理的大小写、标点和轻微形式变化，但不能把语义错误宽松判对。

## 6. 数据模型期望

实现可自行设计数据库结构，但领域模型至少能表达：

- `WordEntry`：规范词元及基础信息。
- `Sense`：稳定 ID、释义、词性、优先级、来源和状态。
- `Phrase`：所属义项、表达、释义、常用程度和标签。
- `ContextMaterial`：目标义项、题型、题面、答案、解释、难度、生成来源、指纹和质量状态。
- `MasteryState`：按用户、义项/短语和理解/产出维度保存掌握状态。
- `ReviewSchedule`：到期时间、间隔、稳定度/难度和调度参数。
- `Attempt`：题目、作答、结果、用时、提示、错误类型及时间。
- `CoverageState`：义项与短语是否已引入、已练习、已验证或已掌握。
- `GenerationJob`：AI 请求状态、输入约束、结果、失败原因和重试信息。

数据模型应支持版本迁移、备份/导出和以后同步扩展。AI 生成内容必须与用户学习记录分离，生成失败或材料被撤销时不能破坏历史进度。

## 7. AI 的职责与边界

AI 可以：

- 为指定词义和搭配生成多样、自然、难度合适的新语境与题目；
- 给出简洁释义、辨析和反馈；
- 标注目标词义、搭配、难度、干扰项理由与答案；
- 对候选材料做重复度、歧义、自然度和目标一致性检查；
- 在确定性规则允许的范围内辅助归类用户错误。

AI 不可以：

- 自行创建不可追踪的义项或覆盖已有规范数据；
- 直接修改用户掌握度、复习时间或历史记录；
- 用一段自由文本代替结构化输出；
- 在不确定时编造事实、词义或不自然搭配；
- 生成缺乏上下文、存在多个合理答案却标成单答案的题目；
- 将用户输入、密钥或不必要的学习数据泄露给外部服务。

所有 AI 输出应经过结构校验、目标词义校验、答案可解性检查、重复检查和基础安全过滤。失败时应丢弃或进入待审状态，并使用已缓存的合格材料继续学习，不阻塞核心流程。

## 8. 产品流程

### 8.1 首次使用

1. 用户进入应用并选择或导入要学的词汇。
2. 系统建立规范词条和义项，不重复导入已有内容。
3. 按优先级生成或准备首批短语与语境。
4. 用少量理解和产出题建立初始能力估计。
5. 形成每日新学与复习队列。

### 8.2 每日学习

1. 首页显示今天待复习数、新内容额度和两个维度的简要进度。
2. 调度器选择下一个知识点和需要训练的维度。
3. 优先使用未见过或近期未出现的合格材料。
4. 用户作答后立即得到简短反馈；需要时展示义项、搭配和解释。
5. 本地引擎更新尝试记录、掌握状态和下次复习时间。
6. 会话结束时展示完成量、薄弱项和覆盖进展。

### 8.3 内容生成

1. 系统根据库存不足、覆盖缺口和即将到期内容创建生成任务。
2. 请求中明确目标义项、短语、题型、难度、禁用的近期材料和结构化输出格式。
3. 返回后执行验证与去重。
4. 合格内容进入题库；失败内容不进入学习队列并可安全重试。

## 9. UX 原则

- 打开应用后能快速开始学习，避免要求用户先完成复杂设置。
- 一次只呈现一个清晰任务，反馈简短、直接且不打断节奏。
- 明确区分“认识”和“会用”，不要只展示一个误导性的总百分比。
- 允许用户查看某个判断为何正确、当前对应哪个词义以及为何需要复习。
- 提示是渐进的，默认不抢先泄露答案。
- 网络或 AI 不可用时给出清楚状态，并继续使用缓存材料复习。
- 不使用羞辱、惩罚性文案或夸大的连续打卡压力。
- 保持无障碍可读性，支持系统深色模式、字体缩放和基本屏幕阅读器语义。

## 10. MVP 范围

首个可用版本必须完成：

- Android 单用户应用；
- 词条、独立义项、短语和语境的创建与浏览；
- 理解与产出两个独立掌握维度；
- 可持久化、可测试的间隔重复调度；
- 理解选择、语境辨义、填空产出、搭配练习和带提示练习；
- 基于到期、弱项和覆盖度的每日自适应队列；
- AI 生成结构化变化材料，以及校验、去重、缓存和失败降级；
- 多义词覆盖状态和避免遗漏/重复的机制；
- 学习历史、基础统计和简要进度页；
- 设置项：每日新词量、每日复习量、AI 服务配置或服务端入口；
- 本地数据导出与恢复；
- 自动化测试与 GitHub Actions 构建。

MVP 不要求账号体系、跨设备实时同步、社交、排行榜、复杂游戏化、教师后台、完整词典编辑器或 iOS/Web 客户端。除非实现核心目标必需，不扩展这些功能。

## 11. 开发顺序

1. 建立可构建的 Android/Kotlin/Compose 项目和持续集成。
2. 完成领域模型、本地存储、迁移与样例数据。
3. 实现确定性的二维掌握模型和间隔重复调度器，并用单元测试固定行为。
4. 实现每日队列与核心答题交互。
5. 实现多义词覆盖、短语关联和材料去重。
6. 接入可替换 AI 接口，完成结构化生成、验证、缓存和离线降级。
7. 完成进度、设置、导入导出、错误处理和无障碍细节。
8. 完善自动化测试、构建产物和版本发布流程。

每一步都保持主分支可构建。重要产品规则应通过测试表达，而不是只存在于界面代码或提示词中。

## 12. 验收标准

满足以下条件才视为 MVP 完成：

- 从空数据开始，用户能添加词汇、完成首次学习、退出并再次进入，进度与复习安排不丢失。
- 同一多义词至少可维护多个独立义项；练习和评分不会串义，已覆盖内容不会被重复创建。
- 对同一知识点连续复习时能看到实质不同的语境，不会仅靠记住固定例句答题。
- 理解题答对不会直接使产出维度达标；两种能力在界面和数据中均独立可见。
- 正确、错误、使用提示和不同反应时间会以合理且稳定的方式影响后续队列；关键调度规则有自动化测试。
- 掌握需要跨时间、跨题型或跨语境的多次证据，单次正确不能直接毕业。
- AI 返回格式错误、语义重复、答案有歧义、网络失败或额度耗尽时，应用不会崩溃，也不会污染有效题库或学习历史。
- AI 不可用时，用户仍能使用缓存材料完成到期复习。
- 所有材料都能追溯到明确目标义项；用户可查看解释并反馈问题材料。
- 基本流程在常见 Android 屏幕尺寸、深色模式和字体放大下可正常使用。
- 测试与构建工作流在全新 GitHub Actions 环境中通过，不依赖开发者本机文件。
- 构建产物可直接从 Actions 下载；创建版本标签后可生成对应 GitHub Release 和 APK。

## 13. GitHub Actions 构建与发布

仓库应提供以下自动化：

### 持续集成

在推送和拉取请求时：

- 使用固定的受支持 JDK 与 Android 构建环境；
- 通过 Gradle Wrapper 执行代码检查、单元测试和 Debug APK 构建；
- 缓存安全的 Gradle 依赖以缩短构建时间；
- 上传测试报告和 Debug APK；
- 任一检查失败时工作流失败。

### 发布

在推送形如 `v*` 的版本标签时：

- 构建 Release APK；
- 使用 GitHub Secrets 中的签名材料完成签名；
- 不在日志或产物中泄露密钥、密码或未加密签名文件；
- 将带版本号的 APK、校验值和必要的版本说明发布到 GitHub Release；
- 若签名配置缺失，应清楚失败，而不是生成看似正式但未签名的发布包。

如后续需要上架 Google Play，可在现有发布流程上扩展 AAB；MVP 首先保证可安装 APK 的稳定产出。

## 14. 完成定义

交付内容包括应用源码、Gradle Wrapper、必要配置、数据库迁移、测试、GitHub Actions 工作流、简洁 README 和可下载构建产物。README 只需说明产品目标、如何配置必要 Secrets、如何由 Actions 构建/发布，以及当前限制；不得要求用户安装本地 Android Studio 才能获得 APK。

实现过程中若产品细节未明确，应优先选择最简单、可测试、可逆且符合本文学习原则的方案，不擅自扩大 MVP。
